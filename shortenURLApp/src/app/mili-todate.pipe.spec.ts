import { MiliTOdatePipe } from './mili-todate.pipe';

describe('MiliTOdatePipe', () => {
  it('create an instance', () => {
    const pipe = new MiliTOdatePipe();
    expect(pipe).toBeTruthy();
  });
});
